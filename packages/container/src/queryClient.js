// container/src/queryClient.js

import { QueryClient } from 'react-query';

const queryClient = new QueryClient();
console.log('Identity of queryClient in queryClient.js:', queryClient);

export default queryClient;
